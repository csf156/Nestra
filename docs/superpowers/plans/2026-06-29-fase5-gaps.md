# Fase 5 Gaps Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Completar las piezas faltantes de Fase 5 (moneda configurable, onboarding de 3 pasos, empty states ilustrados, skeletons en todas las vistas, sparklines en historial, pulido del design-system) sin romper funcionalidad existente.

**Architecture:** App PWA vanilla sin build. Lógica pura nueva como módulo ES testeable (`sparkline.js`); glue UI/moneda como scripts globales clásicos (patrón `format.js`/`iconos.js`). Moneda se resuelve sync desde cache `localStorage` con fallback `S/`. Onboarding es un overlay full-screen mobile-first gatillado en boot por una columna DB. Verificación visual vía preview en viewport móvil (~390px).

**Tech Stack:** JS vanilla (clásico + módulos ES), CSS custom-properties, sprite Tabler, Supabase (Postgres + RLS), `node --test` para lógica pura, MCP `preview_*` para verificación visual.

---

## File Structure

**Crear:**
- `js/moneda.js` — script global: mapa de monedas, getters sync, `setMoneda`, cache desde perfil.
- `js/sparkline.js` — módulo ES (exporta + adjunta a `window`): agregador 7 días + SVG. Testeado.
- `js/ui.js` — script global: `renderEmptyState`, helper de skeleton compartido.
- `views/onboarding.html` — vista overlay 3 pasos (markup + estilos + script inline).
- `test/sparkline.test.mjs` — tests de lógica pura.
- `supabase/migrations/<ts>_fase5_profiles_moneda_onboarding.sql` — columnas nuevas.

**Modificar:**
- `js/format.js` — `formatMonto` lee símbolo/locale de moneda activa.
- `js/db.js` — `updateProfile`/lectura de perfil exponen `moneda` + `onboarding_completado`; ya soportan update genérico.
- `js/router.js` — gatillo de onboarding tras auth.
- `index.html` — incluir `moneda.js`, `sparkline.js`, `ui.js` en orden correcto.
- `css/components.css` — pulido: insight 2px, progress 2px, cat-chip 24×24; estilos onboarding/sparkline/empty-state si faltan.
- `views/configuracion.html` — selector de moneda; empty states; skeleton.
- `views/historial.html` — sparklines; (skeleton ya existe).
- `views/graficos.html`, `views/metas.html`, `views/prestamos.html`, `views/decisiones.html`, `views/resumen.html`, `views/transaccion.html` — skeleton + empty states donde aplique.

---

## Task 1: Migración DB — columnas en `profiles`

**Files:**
- Create: `supabase/migrations/20260629_fase5_profiles_moneda_onboarding.sql`

- [ ] **Step 1: Escribir la migración**

```sql
-- Fase 5: moneda configurable + flag de onboarding por usuario
alter table public.profiles
  add column if not exists moneda text not null default 'PEN';

alter table public.profiles
  add column if not exists onboarding_completado boolean not null default false;
```

- [ ] **Step 2: Aplicar vía MCP supabase**

Usar la tool `mcp__supabase__apply_migration` con name `fase5_profiles_moneda_onboarding` y el SQL de arriba.
Expected: sin error.

- [ ] **Step 3: Verificar**

Tool `mcp__supabase__list_tables` (schemas `["public"]`, verbose true).
Expected: `profiles` ahora tiene columnas `moneda` (text, default `'PEN'`) y `onboarding_completado` (bool, default false).

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260629_fase5_profiles_moneda_onboarding.sql
git commit -m "feat(fase5): migracion profiles moneda + onboarding_completado"
```

---

## Task 2: Lógica pura de sparkline (TDD)

**Files:**
- Create: `js/sparkline.js`
- Test: `test/sparkline.test.mjs`

- [ ] **Step 1: Escribir el test que falla**

```javascript
// test/sparkline.test.mjs
import assert from 'node:assert';
import { test } from 'node:test';
import { agruparGasto7dias, sparklineSVG } from '../js/sparkline.js';

const HOY = new Date(2026, 5, 24); // 24 jun 2026

function gas(monto, fechaISO, categoria_id = 'c1') {
  return { tipo: 'gasto', monto, fecha: fechaISO, categoria_id };
}

test('agruparGasto7dias: 7 valores, hoy es el último', () => {
  const out = agruparGasto7dias([gas(10, '2026-06-24')], 'c1', HOY);
  assert.strictEqual(out.length, 7);
  assert.strictEqual(out[6], 10);   // hoy
  assert.strictEqual(out[0], 0);    // hace 6 días
});

test('agruparGasto7dias: suma por día y filtra por categoría', () => {
  const txs = [
    gas(10, '2026-06-22', 'c1'),
    gas(5,  '2026-06-22', 'c1'),
    gas(99, '2026-06-22', 'c2'), // otra categoría → excluida
    gas(7,  '2026-06-24', 'c1'),
  ];
  const out = agruparGasto7dias(txs, 'c1', HOY);
  assert.strictEqual(out[4], 15); // 22 jun = índice 4 (24-22=2 → 6-2=4)
  assert.strictEqual(out[6], 7);  // 24 jun
});

test('agruparGasto7dias: ignora ingresos y fechas fuera de ventana', () => {
  const txs = [
    { tipo: 'ingreso', monto: 100, fecha: '2026-06-24', categoria_id: 'c1' },
    gas(50, '2026-06-10', 'c1'), // fuera de los 7 días
  ];
  const out = agruparGasto7dias(txs, 'c1', HOY);
  assert.deepStrictEqual(out, [0, 0, 0, 0, 0, 0, 0]);
});

test('sparklineSVG: devuelve <svg> con polyline cuando hay >=2 datos', () => {
  const svg = sparklineSVG([0, 1, 2, 3, 2, 1, 4]);
  assert.match(svg, /^<svg/);
  assert.match(svg, /<polyline/);
  assert.match(svg, /aria-hidden="true"/);
});

test('sparklineSVG: cadena vacía si <2 puntos con dato', () => {
  assert.strictEqual(sparklineSVG([0, 0, 0, 0, 0, 0, 0]), '');
  assert.strictEqual(sparklineSVG([0, 0, 0, 0, 0, 0, 5]), '');
});
```

- [ ] **Step 2: Correr el test para verque falla**

Run: `node --test test/sparkline.test.mjs`
Expected: FAIL — `Cannot find module '../js/sparkline.js'`.

- [ ] **Step 3: Implementar `js/sparkline.js`**

```javascript
// sparkline.js — tendencia 7 días por categoría (lógica pura, sin DOM).
// Módulo ES (testeable) + adjunta helpers a window para scripts clásicos.

// agruparGasto7dias(txs, categoria_id, hoy) → number[7]
// Suma de gasto por día, índice 0 = hace 6 días, índice 6 = hoy.
// hoy: Date local. Compara por fecha YYYY-MM-DD (slice, sin timezone shift).
export function agruparGasto7dias(txs, categoria_id, hoy) {
  const out = [0, 0, 0, 0, 0, 0, 0];
  const claves = [];
  for (let i = 6; i >= 0; i--) {
    const d = new Date(hoy.getFullYear(), hoy.getMonth(), hoy.getDate() - i);
    const iso = d.getFullYear() + '-' +
      String(d.getMonth() + 1).padStart(2, '0') + '-' +
      String(d.getDate()).padStart(2, '0');
    claves.push(iso);
  }
  const idx = new Map(claves.map((k, i) => [k, i]));
  for (const t of (txs || [])) {
    if (t.tipo !== 'gasto') continue;
    if (t.categoria_id !== categoria_id) continue;
    const key = String(t.fecha || '').slice(0, 10);
    if (!idx.has(key)) continue;
    out[idx.get(key)] += Number(t.monto) || 0;
  }
  return out;
}

// sparklineSVG(values, opts) → string SVG inline o '' si no hay señal (>=2 días con dato).
export function sparklineSVG(values, opts = {}) {
  const w = opts.w || 48, h = opts.h || 16, pad = 2;
  const conDato = values.filter((v) => v > 0).length;
  if (conDato < 2) return '';
  const max = Math.max(...values, 1);
  const n = values.length;
  const stepX = (w - pad * 2) / (n - 1);
  const pts = values.map((v, i) => {
    const x = pad + i * stepX;
    const y = h - pad - (v / max) * (h - pad * 2);
    return x.toFixed(1) + ',' + y.toFixed(1);
  }).join(' ');
  return '<svg class="sparkline" width="' + w + '" height="' + h +
    '" viewBox="0 0 ' + w + ' ' + h + '" aria-hidden="true" focusable="false">' +
    '<polyline points="' + pts + '" fill="none" stroke="var(--color-primary)" ' +
    'stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round"/></svg>';
}

// Adjuntar a window para uso en scripts inline de vistas (clásicos).
if (typeof window !== 'undefined') {
  window.agruparGasto7dias = agruparGasto7dias;
  window.sparklineSVG = sparklineSVG;
}
```

- [ ] **Step 4: Correr el test para verificar que pasa**

Run: `node --test test/sparkline.test.mjs`
Expected: PASS — 5 tests.

- [ ] **Step 5: Commit**

```bash
git add js/sparkline.js test/sparkline.test.mjs
git commit -m "feat(fase5): sparkline 7 dias (logica pura + tests)"
```

---

## Task 3: Sistema de moneda — `js/moneda.js`

**Files:**
- Create: `js/moneda.js`
- Modify: `index.html` (incluir antes de `js/format.js`)

- [ ] **Step 1: Crear `js/moneda.js` (script global clásico)**

```javascript
// moneda.js — moneda configurable. Script global clásico (como format.js).
// Resolución sync desde cache localStorage; default PEN. NO convierte divisas:
// solo símbolo + locale + decimales para formateo.
(function () {
  var MONEDAS = {
    PEN: { symbol: 'S/', locale: 'es-PE', decimales: 2 },
    USD: { symbol: '$',  locale: 'en-US', decimales: 2 },
    EUR: { symbol: '€',  locale: 'es-ES', decimales: 2 },
    MXN: { symbol: '$',  locale: 'es-MX', decimales: 2 },
    COP: { symbol: '$',  locale: 'es-CO', decimales: 0 },
    ARS: { symbol: '$',  locale: 'es-AR', decimales: 2 },
    CLP: { symbol: '$',  locale: 'es-CL', decimales: 0 }
  };
  var KEY = 'nestra-moneda';
  var DEFAULT = 'PEN';

  function getMonedaActiva() {
    var code = null;
    try { code = localStorage.getItem(KEY); } catch (e) {}
    return (code && MONEDAS[code]) ? code : DEFAULT;
  }
  function infoMoneda(code) { return MONEDAS[code || getMonedaActiva()] || MONEDAS[DEFAULT]; }
  function monedaSimbolo(code) { return infoMoneda(code).symbol; }
  function monedaLocale(code) { return infoMoneda(code).locale; }
  function monedaDecimales(code) { return infoMoneda(code).decimales; }

  // setMoneda(code) → persiste cache + perfil + evento para re-render.
  async function setMoneda(code) {
    if (!MONEDAS[code]) return;
    try { localStorage.setItem(KEY, code); } catch (e) {}
    if (typeof updateProfile === 'function') {
      try { await updateProfile({ moneda: code }); } catch (e) {}
    }
    window.dispatchEvent(new CustomEvent('nestra:moneda-cambiada', { detail: { code: code } }));
  }
  // cacheMonedaDesdePerfil(perfil) → sincroniza cache desde el perfil cargado en boot.
  function cacheMonedaDesdePerfil(perfil) {
    if (perfil && perfil.moneda && MONEDAS[perfil.moneda]) {
      try { localStorage.setItem(KEY, perfil.moneda); } catch (e) {}
    }
  }

  window.MONEDAS = MONEDAS;
  window.getMonedaActiva = getMonedaActiva;
  window.monedaSimbolo = monedaSimbolo;
  window.monedaLocale = monedaLocale;
  window.monedaDecimales = monedaDecimales;
  window.setMoneda = setMoneda;
  window.cacheMonedaDesdePerfil = cacheMonedaDesdePerfil;
})();
```

- [ ] **Step 2: Incluir en `index.html` antes de `format.js`**

Modify `index.html` — antes de la línea `<script src="js/format.js"></script>` insertar:

```html
    <script src="js/moneda.js"></script>
```

- [ ] **Step 3: Verificar carga (preview)**

Asegurar server: tool `preview_start` (config `nestra`). Luego tool `preview_eval` con
`typeof window.monedaSimbolo === 'function' && window.monedaSimbolo()`.
Expected: `"S/"` (default PEN, sin cache).

- [ ] **Step 4: Commit**

```bash
git add js/moneda.js index.html
git commit -m "feat(fase5): sistema de moneda (moneda.js) con default PEN"
```

---

## Task 4: `formatMonto` usa moneda activa

**Files:**
- Modify: `js/format.js:8-14`

- [ ] **Step 1: Refactor `formatMonto`**

Reemplazar el cuerpo de `formatMonto` en `js/format.js`:

```javascript
function formatMonto(numero) {
  var sym = (typeof monedaSimbolo === 'function') ? monedaSimbolo() : 'S/';
  var loc = (typeof monedaLocale === 'function') ? monedaLocale() : 'es-PE';
  var dec = (typeof monedaDecimales === 'function') ? monedaDecimales() : 2;
  const num = Number(numero);
  if (numero === null || numero === undefined || Number.isNaN(num)) {
    return sym + " 0" + (dec ? "." + "0".repeat(dec) : "");
  }
  return sym + " " + num.toLocaleString(loc, { minimumFractionDigits: dec, maximumFractionDigits: dec });
}
```

- [ ] **Step 2: Verificar default intacto (preview)**

Tool `preview_eval`: `formatMonto(1200)`.
Expected: `"S/ 1,200.00"` (sin cambio respecto al comportamiento previo con PEN).

- [ ] **Step 3: Verificar cambio de moneda (preview)**

Tool `preview_eval`:
`localStorage.setItem('nestra-moneda','USD'); var r=formatMonto(1200.5); localStorage.removeItem('nestra-moneda'); r`
Expected: `"$ 1,200.50"`.

- [ ] **Step 4: Commit**

```bash
git add js/format.js
git commit -m "feat(fase5): formatMonto respeta moneda activa (simbolo/locale/decimales)"
```

---

## Task 5: Helpers UI compartidos — `js/ui.js`

**Files:**
- Create: `js/ui.js`
- Modify: `index.html` (incluir tras `js/iconos.js`)
- Modify: `css/components.css` (estilos `.sparkline`, asegurar `.empty-state` CTA)

- [ ] **Step 1: Crear `js/ui.js`**

```javascript
// ui.js — helpers de UI compartidos (empty states + skeleton). Script global.
(function () {
  function esc(s) {
    return String(s == null ? '' : s)
      .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;');
  }

  // renderEmptyState({icon,title,desc,ctaLabel,ctaHref}) → HTML string.
  // icon: id del sprite Tabler (sin prefijo 'tabler-'); usa <use href="...#tabler-ID">.
  function renderEmptyState(o) {
    o = o || {};
    var icono = o.icon
      ? '<svg class="empty-state-icon" aria-hidden="true"><use href="assets/tabler-sprite.svg#tabler-' + esc(o.icon) + '"></use></svg>'
      : '';
    var cta = (o.ctaLabel && o.ctaHref)
      ? '<a class="btn btn-primary btn-sm empty-state-cta" href="' + esc(o.ctaHref) + '">' + esc(o.ctaLabel) + '</a>'
      : '';
    return '<div class="empty-state">' + icono +
      '<p class="empty-state-title">' + esc(o.title || '') + '</p>' +
      (o.desc ? '<p class="empty-state-desc">' + esc(o.desc) + '</p>' : '') +
      cta + '</div>';
  }

  window.renderEmptyState = renderEmptyState;
})();
```

- [ ] **Step 2: Incluir en `index.html` tras `js/iconos.js`**

Modify `index.html` — después de `<script src="js/iconos.js"></script>` insertar:

```html
    <script src="js/ui.js"></script>
    <script type="module" src="js/sparkline.js"></script>
```

- [ ] **Step 3: Estilos `.sparkline` + CTA en `css/components.css`**

Añadir cerca del bloque `.empty-state` (≈ línea 1049):

```css
.sparkline { display: inline-block; vertical-align: middle; opacity: 0.9; }
.empty-state-cta { margin-top: var(--space-md); }
```

- [ ] **Step 4: Verificar (preview)**

Tool `preview_eval`: `typeof renderEmptyState === 'function' && renderEmptyState({icon:'mood-empty',title:'x'}).includes('empty-state')`.
Expected: `true`.

- [ ] **Step 5: Commit**

```bash
git add js/ui.js index.html css/components.css
git commit -m "feat(fase5): helpers UI (renderEmptyState) + carga sparkline + estilos"
```

---

## Task 6: Onboarding — vista y estilos

**Files:**
- Create: `views/onboarding.html`
- Modify: `css/components.css` (estilos `.onboarding*`)

- [ ] **Step 1: Crear `views/onboarding.html`**

Markup mobile-first, overlay full-screen. Tres `<section class="ob-step">` (solo una `.is-active`), dots de progreso, montos en `.signature-num` (Playfair). Script inline maneja navegación y persistencia. Usa funciones globales existentes: `getCategorias`, `insertCategoria`, `upsertRecurrente`, `insertTransaccion`, `updateProfile`, `setMoneda`, `iconoCategoria`, `formatMonto`, `navigateTo`.

```html
<div id="onboarding" class="onboarding" role="dialog" aria-modal="true" aria-label="Configuración inicial">
  <div class="ob-card">
    <div class="ob-dots" aria-hidden="true">
      <span class="ob-dot is-active"></span><span class="ob-dot"></span><span class="ob-dot"></span>
    </div>

    <!-- Paso 1: moneda + ingreso -->
    <section class="ob-step is-active" data-step="1">
      <h1 class="ob-title">Bienvenido a Nestra</h1>
      <p class="ob-desc">Empecemos por tu moneda y tu ingreso mensual.</p>
      <label class="ob-field">
        <span>Moneda</span>
        <select id="obMoneda" class="input"></select>
      </label>
      <label class="ob-field">
        <span>Ingreso mensual</span>
        <input id="obIngreso" class="input" type="number" inputmode="decimal" min="0" step="0.01" placeholder="0.00">
      </label>
      <div class="ob-actions">
        <button class="btn btn-primary ob-next" data-next="2" type="button">Continuar</button>
      </div>
    </section>

    <!-- Paso 2: categorías -->
    <section class="ob-step" data-step="2">
      <h1 class="ob-title">Tus categorías</h1>
      <p class="ob-desc">Elige al menos 3 para empezar. Puedes ajustarlas luego.</p>
      <div id="obCats" class="ob-cats"></div>
      <div class="ob-actions">
        <button class="btn btn-ghost ob-back" data-back="1" type="button">Atrás</button>
        <button class="btn btn-primary ob-next" data-next="3" type="button">Continuar</button>
      </div>
    </section>

    <!-- Paso 3: transacción ejemplo -->
    <section class="ob-step" data-step="3">
      <h1 class="ob-title">Tu primer gasto</h1>
      <p class="ob-desc">Registra un gasto de ejemplo para ver Nestra con vida.</p>
      <label class="ob-field">
        <span>Categoría</span>
        <select id="obTxCat" class="input"></select>
      </label>
      <label class="ob-field">
        <span>Monto</span>
        <input id="obTxMonto" class="input" type="number" inputmode="decimal" min="0" step="0.01" placeholder="0.00">
      </label>
      <div class="ob-actions">
        <button class="btn btn-ghost ob-back" data-back="2" type="button">Atrás</button>
        <button class="btn btn-ghost ob-skip" type="button">Omitir</button>
        <button class="btn btn-primary ob-finish" type="button">Empezar</button>
      </div>
    </section>
  </div>
</div>

<script>
(function () {
  function $(id) { return document.getElementById(id); }
  var root = $('onboarding');
  if (!root) return;
  var creadas = []; // categorías creadas en paso 2

  // Poblar monedas
  var selM = $('obMoneda');
  Object.keys(window.MONEDAS || { PEN: 1 }).forEach(function (code) {
    var o = document.createElement('option');
    o.value = code; o.textContent = code + ' (' + window.MONEDAS[code].symbol + ')';
    selM.appendChild(o);
  });
  selM.value = (typeof getMonedaActiva === 'function') ? getMonedaActiva() : 'PEN';

  // Chips sugeridos (nombre + icono Tabler + color)
  var SUGERIDAS = [
    { nombre: 'Comida', icono: 'tabler-tools-kitchen-2', color: '#e8b94a' },
    { nombre: 'Transporte', icono: 'tabler-car', color: '#6ea8e8' },
    { nombre: 'Hogar', icono: 'tabler-home', color: '#5ec98a' },
    { nombre: 'Ocio', icono: 'tabler-device-gamepad-2', color: '#c9a84c' },
    { nombre: 'Salud', icono: 'tabler-heart', color: '#f08a8a' },
    { nombre: 'Servicios', icono: 'tabler-bolt', color: '#a8a29a' }
  ];
  var elegidas = new Set();
  var cont = $('obCats');
  SUGERIDAS.forEach(function (c, i) {
    var b = document.createElement('button');
    b.type = 'button'; b.className = 'ob-cat-chip'; b.dataset.i = i;
    b.innerHTML = (typeof iconoCategoria === 'function'
      ? iconoCategoria(c.nombre, { id: c.icono }) : '') + '<span>' + c.nombre + '</span>';
    b.addEventListener('click', function () {
      if (elegidas.has(i)) { elegidas.delete(i); b.classList.remove('is-on'); }
      else { elegidas.add(i); b.classList.add('is-on'); }
    });
    cont.appendChild(b);
  });

  function goto(step) {
    root.querySelectorAll('.ob-step').forEach(function (s) {
      s.classList.toggle('is-active', s.dataset.step === String(step));
    });
    var dots = root.querySelectorAll('.ob-dot');
    dots.forEach(function (d, i) { d.classList.toggle('is-active', i < step); });
  }

  root.querySelectorAll('.ob-back').forEach(function (btn) {
    btn.addEventListener('click', function () { goto(Number(btn.dataset.back)); });
  });

  // Paso 1 → 2: guardar moneda + recurrente ingreso
  root.querySelector('[data-next="2"]').addEventListener('click', async function () {
    var code = selM.value;
    if (typeof setMoneda === 'function') await setMoneda(code);
    var ing = Number($('obIngreso').value);
    if (ing > 0 && typeof upsertRecurrente === 'function') {
      try {
        await upsertRecurrente({
          descripcion: 'Ingreso mensual', monto: ing, tipo: 'ingreso',
          frecuencia: 'mensual', dia_cargo: 1, activo: true
        });
      } catch (e) {}
    }
    goto(2);
  });

  // Paso 2 → 3: crear categorías elegidas (evitar duplicados por nombre)
  root.querySelector('[data-next="3"]').addEventListener('click', async function () {
    if (elegidas.size < 3) { alert('Elige al menos 3 categorías.'); return; }
    creadas = [];
    var existentes = [];
    try { existentes = (await getCategorias('gasto')) || []; } catch (e) {}
    var nombresExist = new Set(existentes.map(function (c) { return (c.nombre || '').toLowerCase(); }));
    var items = Array.from(elegidas).map(function (i) { return SUGERIDAS[i]; });
    for (var k = 0; k < items.length; k++) {
      var c = items[k];
      if (nombresExist.has(c.nombre.toLowerCase())) {
        var ya = existentes.find(function (e) { return (e.nombre || '').toLowerCase() === c.nombre.toLowerCase(); });
        if (ya) creadas.push(ya);
        continue;
      }
      try {
        var nueva = await insertCategoria({
          nombre: c.nombre, tipo: 'gasto', icono: c.icono, color: c.color, estado: 'activa'
        });
        if (nueva) creadas.push(nueva);
      } catch (e) {}
    }
    // poblar select del paso 3
    var sel = $('obTxCat'); sel.innerHTML = '';
    creadas.forEach(function (c) {
      var o = document.createElement('option'); o.value = c.id; o.textContent = c.nombre; sel.appendChild(o);
    });
    goto(3);
  });

  async function finalizar(crearTx) {
    if (crearTx) {
      var monto = Number($('obTxMonto').value);
      var catId = $('obTxCat').value;
      if (monto > 0 && catId && typeof insertTransaccion === 'function') {
        var hoy = new Date();
        var iso = hoy.getFullYear() + '-' + String(hoy.getMonth() + 1).padStart(2, '0') + '-' + String(hoy.getDate()).padStart(2, '0');
        try {
          await insertTransaccion({ tipo: 'gasto', ambito: 'personal', categoria_id: catId, monto: monto, fecha: iso });
        } catch (e) {}
      }
    }
    if (typeof updateProfile === 'function') {
      try { await updateProfile({ onboarding_completado: true }); } catch (e) {}
    }
    try { localStorage.setItem('nestra-onboarding-done', '1'); } catch (e) {}
    root.remove();
    if (typeof navigateTo === 'function') navigateTo('#dashboard'); else location.hash = '#dashboard';
  }

  root.querySelector('.ob-finish').addEventListener('click', function () { finalizar(true); });
  root.querySelector('.ob-skip').addEventListener('click', function () { finalizar(false); });
})();
</script>
```

- [ ] **Step 2: Estilos `.onboarding*` en `css/components.css`**

Añadir al final de `css/components.css`:

```css
/* ── Onboarding (Fase 5) ─────────────────────────────────────── */
.onboarding {
  position: fixed; inset: 0; z-index: 9998;
  display: flex; align-items: center; justify-content: center;
  padding: var(--space-lg); background: var(--bg-light);
  overflow-y: auto;
}
.ob-card { width: 100%; max-width: 28rem; }
.ob-dots { display: flex; gap: var(--space-sm); justify-content: center; margin-bottom: var(--space-xl); }
.ob-dot { width: 28px; height: 2px; background: var(--border-light); border-radius: 1px; transition: background 0.2s; }
.ob-dot.is-active { background: var(--color-primary); }
.ob-step { display: none; flex-direction: column; gap: var(--space-md); }
.ob-step.is-active { display: flex; }
.ob-title { font-family: var(--font-display); font-weight: 400; }
.ob-desc { color: var(--text-secondary); }
.ob-field { display: flex; flex-direction: column; gap: var(--space-xs); font-size: var(--font-size-sm); }
.ob-cats { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-sm); }
.ob-cat-chip {
  display: flex; align-items: center; gap: var(--space-sm);
  padding: var(--space-sm) var(--space-md); border: 1px solid var(--border-light);
  border-radius: var(--radius-md); background: var(--bg-light-secondary);
  color: var(--text-dark); cursor: pointer; text-align: left;
}
.ob-cat-chip svg { width: 18px; height: 18px; }
.ob-cat-chip.is-on { border-color: var(--color-primary); background: color-mix(in srgb, var(--color-primary) 8%, transparent); }
.ob-actions { display: flex; gap: var(--space-sm); margin-top: var(--space-lg); }
.ob-actions .btn-primary { margin-left: auto; }
```

- [ ] **Step 3: Commit**

```bash
git add views/onboarding.html css/components.css
git commit -m "feat(fase5): vista onboarding 3 pasos + estilos editorial mobile-first"
```

---

## Task 7: Gatillo de onboarding en boot

**Files:**
- Modify: `js/router.js` (en `handleRouteChange`, rama autenticada)

- [ ] **Step 1: Localizar el punto de inserción**

En `js/router.js`, dentro de `handleRouteChange`, tras confirmar sesión y antes de `loadView(route.view)` (≈ línea 184). El gatillo solo corre en rutas protegidas con sesión.

- [ ] **Step 2: Añadir la función de gatillo**

Agregar cerca del top de `js/router.js` (tras `navigateTo`):

```javascript
// mostrarOnboardingSiHaceFalta() — monta el overlay si el perfil no lo completó.
// No bloquea la app si el perfil no carga (offline sin cache previo).
async function mostrarOnboardingSiHaceFalta() {
  if (document.getElementById('onboarding')) return false;
  try { if (localStorage.getItem('nestra-onboarding-done') === '1') return false; } catch (e) {}
  if (typeof getProfiles !== 'function') return false;
  let perfil = null;
  try {
    const ses = (typeof getSessionUserId === 'function') ? getSessionUserId() : null;
    const profs = await getProfiles();
    perfil = (profs || []).find((p) => !ses || p.user_id === ses) || (profs || [])[0] || null;
  } catch (e) { return false; }
  if (!perfil) return false;
  if (typeof cacheMonedaDesdePerfil === 'function') cacheMonedaDesdePerfil(perfil);
  if (perfil.onboarding_completado) {
    try { localStorage.setItem('nestra-onboarding-done', '1'); } catch (e) {}
    return false;
  }
  const host = document.getElementById('app') || document.body;
  try {
    const html = await fetch('views/onboarding.html').then((r) => r.text());
    const wrap = document.createElement('div');
    wrap.innerHTML = html;
    Array.prototype.slice.call(wrap.querySelectorAll('script')).forEach(function (old) {
      const s = document.createElement('script');
      if (old.src) s.src = old.src; else s.textContent = old.textContent;
      old.parentNode.replaceChild(s, old);
    });
    document.body.appendChild(wrap);
    return true;
  } catch (e) { return false; }
}
```

Nota: si `getSessionUserId` no existe en el codebase, usar la fuente de user-id que ya use `_requireUserId`/`auth.js` (revisar `js/auth.js` y replicar). El `find` ya degrada a primer perfil.

- [ ] **Step 3: Invocar en `handleRouteChange`**

En la rama autenticada de rutas protegidas, antes de `await loadView(route.view);` añadir:

```javascript
    if (!isPublic) {
      const ob = await mostrarOnboardingSiHaceFalta();
      if (ob) return; // onboarding tomó la pantalla; al terminar navega a #dashboard
    }
```

- [ ] **Step 4: Verificar (preview, cuenta de prueba)**

1. `preview_start`. Login con cuenta de prueba (ver memoria `nestra-v2-test-account`).
2. Resetear flag para forzar onboarding: tool `mcp__supabase__execute_sql`:
   `update public.profiles set onboarding_completado = false;` y en `preview_eval`: `localStorage.removeItem('nestra-onboarding-done')`.
3. `preview_eval`: `location.hash='#dashboard'; location.reload()` (o navegar).
4. `preview_snapshot`: confirmar overlay onboarding visible con paso 1.
5. `preview_resize` 390×844 (móvil): confirmar sin scroll horizontal.

Expected: overlay visible, paso 1 con select moneda + input ingreso, dots (1/3).

- [ ] **Step 5: Verificar flujo completo (preview)**

`preview_click` Continuar → paso 2; elegir 3 chips; Continuar → paso 3; llenar monto; Empezar.
Luego `preview_eval`: `!!document.getElementById('onboarding')` → `false`; y verificar dashboard muestra datos.
`mcp__supabase__execute_sql`: `select onboarding_completado from profiles;` → `true`.

- [ ] **Step 6: Commit**

```bash
git add js/router.js
git commit -m "feat(fase5): gatillo de onboarding en boot (una sola vez por perfil)"
```

---

## Task 8: Selector de moneda en configuración

**Files:**
- Modify: `views/configuracion.html` (sección de preferencias, junto al tema en ≈ línea 1431)

- [ ] **Step 1: Añadir markup del selector**

En la sección de ajustes/preferencias (donde está el toggle de tema), añadir:

```html
<label class="cfg-field">
  <span>Moneda</span>
  <select id="cfgMoneda" class="input"></select>
</label>
```

- [ ] **Step 2: Cablear el select en el script inline de la vista**

Dentro del IIFE de `configuracion.html`, tras la lógica de tema:

```javascript
var selMon = document.getElementById('cfgMoneda');
if (selMon && window.MONEDAS) {
  Object.keys(window.MONEDAS).forEach(function (code) {
    var o = document.createElement('option');
    o.value = code; o.textContent = code + ' (' + window.MONEDAS[code].symbol + ')';
    selMon.appendChild(o);
  });
  selMon.value = (typeof getMonedaActiva === 'function') ? getMonedaActiva() : 'PEN';
  selMon.addEventListener('change', function () {
    if (typeof setMoneda === 'function') setMoneda(selMon.value);
  });
}
```

- [ ] **Step 3: Verificar (preview)**

`preview_start`; navegar a `#configuracion`; `preview_snapshot` confirma el select.
`preview_eval` cambiar a USD vía la UI o `setMoneda('USD')`; navegar a dashboard; `preview_snapshot` montos en `$`.
Restaurar: `setMoneda('PEN')`.

- [ ] **Step 4: Commit**

```bash
git add views/configuracion.html
git commit -m "feat(fase5): selector de moneda en configuracion"
```

---

## Task 9: Empty states ilustrados en las vistas

**Files:**
- Modify: `views/historial.html`, `views/graficos.html`, `views/metas.html`, `views/prestamos.html`, `views/configuracion.html`, `views/dashboard.html`

Para cada vista: localizar el punto donde hoy se renderiza lista/tabla vacía (texto tipo "Sin …"/"No hay …") y reemplazar por `renderEmptyState(...)`. Íconos Tabler (sin prefijo `tabler-`).

- [ ] **Step 1: historial — sin transacciones**

Donde se pinta el cuerpo vacío de la lista:

```javascript
contenedor.innerHTML = renderEmptyState({
  icon: 'receipt-off', title: 'Sin movimientos todavía',
  desc: 'Registra tu primer gasto o ingreso para verlo aquí.',
  ctaLabel: 'Registrar', ctaHref: '#transaccion'
});
```

- [ ] **Step 2: graficos — sin datos del periodo**

```javascript
contenedor.innerHTML = renderEmptyState({
  icon: 'chart-dots', title: 'Aún no hay datos para graficar',
  desc: 'Cuando registres movimientos, verás tus tendencias aquí.',
  ctaLabel: 'Registrar', ctaHref: '#transaccion'
});
```

- [ ] **Step 3: metas — sin metas**

```javascript
contenedor.innerHTML = renderEmptyState({
  icon: 'target', title: 'Sin metas aún',
  desc: 'Crea tu primera meta de ahorro y haz seguimiento.',
  ctaLabel: 'Crear meta', ctaHref: '#metas?nueva=1'
});
```

(Ajustar `ctaHref` al patrón real de "nueva meta" de esa vista; si abre modal, usar un botón con el handler existente en lugar de href.)

- [ ] **Step 4: prestamos — sin préstamos**

```javascript
contenedor.innerHTML = renderEmptyState({
  icon: 'cash-banknote', title: 'Sin préstamos registrados',
  desc: 'Registra dinero que prestaste para no perderle el rastro.',
  ctaLabel: 'Registrar', ctaHref: '#transaccion'
});
```

- [ ] **Step 5: configuracion — listas vacías (categorías/recurrentes/plantillas)**

Donde hoy hay strings "Sin categorías…"/"Sin recurrentes…", sustituir por `renderEmptyState` con `icon` apropiado (`category`, `repeat`, `template`) sin CTA (la acción de alta ya está en la misma card).

- [ ] **Step 6: dashboard — estado sin datos**

Si el dashboard no tiene movimientos, mostrar `renderEmptyState({icon:'sparkles', title:'Tu panel está listo', desc:'Registra tu primer movimiento para ver tu resumen.', ctaLabel:'Registrar', ctaHref:'#transaccion'})` en el contenedor principal.

- [ ] **Step 7: Verificar (preview, sin datos)**

Con una cuenta/estado sin datos (o filtro que no arroje resultados), navegar a cada vista y `preview_snapshot` confirma empty state con ícono + título + CTA (no tabla en blanco). `preview_resize` 390px: sin overflow.

- [ ] **Step 8: Commit**

```bash
git add views/historial.html views/graficos.html views/metas.html views/prestamos.html views/configuracion.html views/dashboard.html
git commit -m "feat(fase5): empty states ilustrados (Tabler) en todas las vistas"
```

---

## Task 10: Skeletons en las vistas restantes

**Files:**
- Modify: `views/graficos.html`, `views/metas.html`, `views/prestamos.html`, `views/configuracion.html`, `views/decisiones.html`, `views/resumen.html`, `views/transaccion.html`

Patrón de referencia: `views/dashboard.html` y `views/historial.html` (ya usan `.skeleton*`). Reemplazar el spinner/`loading-overlay` de carga inicial por markup skeleton que imita el layout real. Conservar `.btn .spinner` (acciones).

- [ ] **Step 1: graficos**

Reemplazar el contenedor de carga por skeletons de cards de gráfico:

```html
<div class="skeleton-card"></div>
<div class="skeleton-card"></div>
```

Mostrarlos mientras se cargan datos; ocultarlos al pintar (misma lógica que hoy oculta el spinner).

- [ ] **Step 2: metas**

```html
<div class="skeleton-row"><div class="skeleton-avatar"></div><div class="skeleton-row-content"><div class="skeleton-text skeleton-title"></div><div class="skeleton-text"></div></div></div>
<div class="skeleton-row"><div class="skeleton-avatar"></div><div class="skeleton-row-content"><div class="skeleton-text skeleton-title"></div><div class="skeleton-text"></div></div></div>
```

- [ ] **Step 3: prestamos** — usar el mismo patrón `.skeleton-row` ×3 que metas.

- [ ] **Step 4: configuracion** — `.skeleton-row` ×3 dentro de cada card que cargue listas async (categorías/recurrentes).

- [ ] **Step 5: decisiones** — `.skeleton-card` ×1-2 según el layout de la vista.

- [ ] **Step 6: resumen** — `.skeleton-text skeleton-title` + `.skeleton-card` imitando los bloques de resumen.

- [ ] **Step 7: transaccion** — `.skeleton-row` para el form/lista que cargue async (si aplica; si la carga es instantánea, omitir y dejar nota).

- [ ] **Step 8: Verificar (preview)**

Para cada vista: `preview_start`, navegar, e inmediatamente `preview_snapshot` (o throttling vía `preview_network` si disponible) para captar el skeleton antes del contenido. Confirmar que ya NO aparece el spinner de carga de vista. `preview_console_logs` sin errores.

- [ ] **Step 9: Commit**

```bash
git add views/graficos.html views/metas.html views/prestamos.html views/configuracion.html views/decisiones.html views/resumen.html views/transaccion.html
git commit -m "feat(fase5): skeletons en todas las vistas (reemplazan spinners de carga)"
```

---

## Task 11: Sparklines inline en historial

**Files:**
- Modify: `views/historial.html` (render de filas/grupos por categoría)

- [ ] **Step 1: Asegurar acceso a las transacciones del periodo**

En el script inline de `historial.html`, identificar el array de transacciones ya cargado para el render (el que alimenta la lista) y la fecha de referencia (hoy).

- [ ] **Step 2: Insertar sparkline junto al chip de categoría**

Donde se construye cada fila/grupo con `.cat-chip`, añadir tras el nombre de categoría:

```javascript
var spark = '';
if (typeof agruparGasto7dias === 'function' && typeof sparklineSVG === 'function' && row.categoria_id) {
  var serie = agruparGasto7dias(txsDelPeriodo, row.categoria_id, new Date());
  spark = sparklineSVG(serie);
}
// ... insertar `spark` en el HTML de la fila, junto a la categoría
```

(Adaptar `txsDelPeriodo` y `row` a los nombres reales del script. La sparkline es decorativa: `aria-hidden`.)

- [ ] **Step 3: Verificar (preview)**

Con datos de gasto en ≥2 días distintos para una categoría: navegar a `#historial`, `preview_snapshot` confirma `<svg class="sparkline">` junto a la categoría. `preview_resize` 390px: no rompe el layout de la fila. Categorías con <2 días con dato → sin sparkline (no línea plana).

- [ ] **Step 4: Commit**

```bash
git add views/historial.html
git commit -m "feat(fase5): sparklines de 7 dias por categoria en historial"
```

---

## Task 12: Pulido design-system (2px / 24×24)

**Files:**
- Modify: `css/components.css`

- [ ] **Step 1: Insight cards border-left 3px → 2px**

En `css/components.css` (≈ línea 836), cambiar `border-left: 3px solid transparent;` a `border-left: 2px solid transparent;`. (No tocar `.toast` que usa border-left semántico de 3px salvo que se quiera unificar — dejar toast como está.)

- [ ] **Step 2: Progress bars finas (2px) por defecto**

`.progress` (≈ línea 796): fijar `height: 2px;` como default. Conservar `.progress-md` (6px) y `.progress-lg` (10px) para usos que requieran grosor semántico.

- [ ] **Step 3: cat-chip caja 24×24**

`.cat-chip` (≈ línea 1326): asegurar caja 24×24 (`width:24px;height:24px;` para la variante ícono-only, padding 0, centrado). Mantener `.cat-chip svg { width:14px; height:14px; }`.

- [ ] **Step 4: Verificar (preview)**

Navegar a presupuestos/metas (progress) y a una vista con insight cards y cat-chips. `preview_inspect` sobre `.progress` → height 2px; sobre `.cat-chip` → 24×24. `preview_snapshot` confirma estética fina.

- [ ] **Step 5: Commit**

```bash
git add css/components.css
git commit -m "style(fase5): barras 2px, insight 2px, cat-chip 24x24 (pulido editorial)"
```

---

## Task 13: Auditoría WCAG AA (skill accessibility)

**Files:**
- Modify: `css/base.css` (solo si algún token no alcanza el ratio)

- [ ] **Step 1: Invocar la skill `accessibility`**

Auditar contraste de los pares clave (dark y light):
- texto principal `--text-dark` sobre `--bg-light`
- texto secundario `--text-secondary` (`#a8a29a`) sobre `--bg-light` (`#08080f`)
- champagne `--color-primary` (`#c9a84c`) sobre `#08080f` (links/nav/CTA texto)
- estados semánticos success/danger/warning/info sobre fondo
- texto oscuro sobre champagne (botón primario)

Umbrales: 4.5:1 texto normal, 3:1 texto grande / componentes UI / trazos de gráfico.

- [ ] **Step 2: Ajustar tokens que no alcancen**

Si un par falla, subir luminancia del token en `css/base.css` (p. ej. aclarar `--text-secondary` o `--color-primary`) manteniendo la identidad champagne. Documentar el ratio resultante en el commit.

- [ ] **Step 3: Verificar (preview)**

`preview_inspect` de los colores computados; confirmar ratios con la skill. Repetir en light mode (`html.light`).

- [ ] **Step 4: Commit (si hubo cambios)**

```bash
git add css/base.css
git commit -m "fix(fase5): ajustes de contraste WCAG AA (dark + light)"
```

---

## Task 14: Verificación integral + deploy

- [ ] **Step 1: Correr la suite de tests**

Run: `node --test test/`
Expected: todos PASS (incluye `sparkline.test.mjs`).

- [ ] **Step 2: Recorrido mobile en preview**

`preview_start`; `preview_resize` 390×844. Recorrer dashboard, historial (sparklines), graficos, metas, prestamos, configuracion (selector moneda), decisiones, resumen. En cada una: `preview_console_logs` sin errores, sin scroll horizontal, skeleton al cargar, empty state si sin datos.

- [ ] **Step 3: Restaurar estado de la cuenta de prueba**

Si se reseteó `onboarding_completado`, dejar el estado coherente para el usuario real (no resetear el perfil del usuario; solo la cuenta de prueba). `setMoneda('PEN')` si se cambió.

- [ ] **Step 4: Push a v2 y verificar deploy live**

```bash
git push origin v2
```

Esperar build Pages (~1-2 min). Verificar:
```bash
curl -sL https://nestra-8rl.pages.dev/sw.js | grep SHELL_VERSION
```

Nota: si se añadieron assets nuevos al shell precacheado (`moneda.js`, `ui.js`, `sparkline.js`, `views/onboarding.html`), bumpear `SHELL_VERSION` y la lista de precache en `sw.js` (Step 5) ANTES del push.

- [ ] **Step 5: Bump SW (si aplica)**

Revisar `sw.js`: añadir los archivos nuevos a la precache list si corresponde y subir `SHELL_VERSION`. Commit + push.

```bash
git add sw.js
git commit -m "chore(fase5): bump SHELL_VERSION + precache assets nuevos"
git push origin v2
```

---

## Self-Review (cobertura del spec)

- A. Moneda → Tasks 1, 3, 4, 8 ✅
- B. Onboarding → Tasks 1, 6, 7 ✅
- C. Empty states → Tasks 5, 9 ✅
- D. Skeletons → Task 10 ✅
- E. Sparklines → Tasks 2, 11 ✅
- F. Pulido + WCAG → Tasks 12, 13 ✅
- Mobile-first → verificación 390px en Tasks 7, 9, 10, 11, 14 ✅
- No duplicar → reusa `insertCategoria`/`upsertRecurrente`/`insertTransaccion`/`updateProfile`/`getCategorias`/sprite/CSS existentes ✅

Consistencia de nombres: `agruparGasto7dias`, `sparklineSVG`, `renderEmptyState`, `getMonedaActiva`, `monedaSimbolo`, `monedaLocale`, `monedaDecimales`, `setMoneda`, `cacheMonedaDesdePerfil`, `mostrarOnboardingSiHaceFalta` — usados consistentemente entre tasks.
